---
"@cosmonexus/nova-emotion": minor
"@cosmonexus/design-tokens": patch
---

Add `@cosmonexus/nova-emotion`, a thin Emotion adapter over Cosmonexus design tokens. `nova-ui` stays on vanilla-extract. Mark design-token CSS files as side-effectful so bundlers keep `--cnx-*` custom properties.
