# @cosmonexus/nova-emotion

## 0.1.1

### Patch Changes

- Depend on `@cosmonexus/design-tokens@^0.1.1` so token CSS stays side-effectful.

## 0.1.0

### Minor Changes

- Initial Emotion adapter for Cosmonexus design tokens (`css` / `cx` / `theme` plus a token CSS side-effect entry).
