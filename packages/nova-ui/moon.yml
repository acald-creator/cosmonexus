type: library
language: typescript

tasks:
  build:
    command: tsdown
    inputs:
      - src/**/*
      - tsdown.config.ts
      - package.json
    outputs:
      - dist/**/*
    deps:
      - ^:build

  test:
    command: vitest run
    inputs:
      - src/**/*
      - vitest.config.ts
    deps:
      - ~:build

  lint:
    command: biome check .
    inputs:
      - src/**/*

  typecheck:
    command: tsc --noEmit
    inputs:
      - src/**/*
      - tsconfig.json
    deps:
      - ^:build
