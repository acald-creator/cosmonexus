layer: library
language: typescript

dependsOn:
  - design-tokens

tasks:
  build:
    command: bunx svelte-package
    inputs:
      - src/lib/**/*
      - svelte.config.js
      - package.json
    outputs:
      - dist/**/*
    deps:
      - ^:build

  lint:
    command: biome check .
    inputs:
      - src/lib/**/*

  typecheck:
    command: bunx svelte-check --tsconfig ./tsconfig.json
    inputs:
      - src/lib/**/*
      - tsconfig.json
    deps:
      - ^:build
